export type Metadata = {
    title: string
    description: string
    keywords?: string[]
}

export type NavigationItem = {
    id: string
    text: string
    path: string
    icon: string
    activeBgColor: string
    borderColor: string
}
